# Telegram Chats Manager

Приложение на Python для управления и просмотра чатов и каналов Telegram с использованием customtkinter.

## Возможности

- 🔐 Авторизация в Telegram с сохранением сессии
- 💾 Автоматическое сохранение списка чатов и каналов в базу данных SQLite
- 📊 Отображение чатов в удобной таблице с темной темой
- 🔄 Обновление списка чатов одним нажатием кнопки
- 🚫 Исключение личных чатов из списка

## Требования

- Python 3.8 или выше
- Telegram API credentials (api_id и api_hash)

## Установка

1. Клонируйте репозиторий или скачайте проект.

2. Создайте виртуальное окружение:
```bash
python -m venv venv
```

3. Активируйте виртуальное окружение:
   - Windows:
     ```bash
     venv\Scripts\activate
     ```
   - Linux/MacOS:
     ```bash
     source venv/bin/activate
     ```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Получение Telegram API credentials

1. Перейдите на https://my.telegram.org/auth
2. Войдите используя ваш номер телефона
3. Перейдите в раздел "API development tools"
4. Создайте новое приложение и получите `api_id` и `api_hash`

## Настройка

Укажите ваши Telegram API credentials одним из способов:

### Способ 1: Переменные окружения (рекомендуется)

**Windows (PowerShell):**
```powershell
$env:TELEGRAM_API_ID="ваш_api_id"
$env:TELEGRAM_API_HASH="ваш_api_hash"
```

**Windows (CMD):**
```cmd
set TELEGRAM_API_ID=ваш_api_id
set TELEGRAM_API_HASH=ваш_api_hash
```

**Linux/MacOS:**
```bash
export TELEGRAM_API_ID="ваш_api_id"
export TELEGRAM_API_HASH="ваш_api_hash"
```

### Способ 2: Редактирование файла telegram_logic.py

Откройте `telegram_logic.py` и замените пустые строки на ваши значения:
```python
API_ID = 'ваш_api_id'
API_HASH = 'ваш_api_hash'
```

## Запуск

После активации виртуального окружения и настройки API credentials:

```bash
python main.py
```

При первом запуске приложение запросит:
1. Номер телефона
2. Код подтверждения из Telegram
3. Пароль двухфакторной аутентификации (если включена)

Сессия будет сохранена в папке `data/session.session`, поэтому при последующих запусках авторизация не потребуется.

## Использование

1. При первом запуске нажмите кнопку "Обновить" для получения списка чатов
2. Список чатов и каналов отобразится в таблице
3. Для обновления списка нажмите кнопку "Обновить" повторно

## Структура проекта

```
tg/
├── data/                  # Папка для хранения БД и сессий
│   ├── chats.db          # База данных SQLite с чатами
│   └── session.session   # Файл сессии Telegram
├── database.py           # Модуль для работы с БД
├── telegram_logic.py     # Модуль для работы с Telegram API
├── main.py              # Главный файл приложения
├── requirements.txt     # Зависимости проекта
└── README.md           # Документация
```

## База данных

База данных SQLite (`data/chats.db`) содержит таблицу `chats` со следующими полями:

- `id` - INTEGER PRIMARY KEY (автоинкремент)
- `tg_id` - INTEGER UNIQUE (ID чата в Telegram)
- `title` - TEXT (Название чата/канала)

## Безопасность

⚠️ **Важно:**
- Не публикуйте ваши `api_id` и `api_hash` в публичных репозиториях
- Файл `data/session.session` содержит данные авторизации - храните его в безопасности
- Папка `data/` добавлена в `.gitignore` для защиты данных

## Лицензия

Этот проект создан в образовательных целях.

